package src.aircraft;

import src.weather.Coordinates;

public class test {
  public static void main() {
    Coordinates coordinates = new Coordinates(1, 2, 3);
    System.out.println(coordinates);
    // Aircraft aircraft = new Aircraft("", coordinates);
  }
}